{
  "name": "feature-request",
  "version": "0.1.0",
  "description": "MCP tool for agents to record feature suggestions into feature_requests.toml files",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "node server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@iarna/toml": "^2.2.5"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^6.0.2"
  }
}
